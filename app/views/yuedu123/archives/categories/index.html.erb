<div class="main-content">
  <div class="list">
    <% if @archives_categories.any? %>
      <% @archives_categories.each do |category| %>
    <div class="item">
      <div class="operate">
      </div>        
      <%= link_to "#{category.level_with_name}", 
        archives_route(yuedu123_archives_category_path(:url_name => category.url_name)) %>
    </div>
      <% end %>
    <% else %>
      暂无分类
    <% end %>
  </div>    
</div>